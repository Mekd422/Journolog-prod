import { AboutClient } from "./AboutClient";

export const metadata = {
  title: "About Us | Journolog",
  description: "Learn more about the story, inspiration, and creator behind Journolog.",
};

export default function AboutPage() {
  return <AboutClient />;
}
